#include <stdio.h>
#include <math.h>

int main() {

    // int ans = ceil(4.1);
    // int ans = floor(4.9);
    // int ans = round(4.5);
    // int ans = round(4.3);
    // float ans = sqrt(132);
    // int ans = pow(2, 4);
    int ans = abs(-10);

    printf("%d", ans);
    // printf("%f", ans);
    
    return 0;
}
